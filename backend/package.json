{
  "name": "metro-now-server",
  "module": "src/server.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "start": "bun run src/server.ts",
    "start:dev": "bun --watch src/server.ts",
    "start:build": "bun run src/server.js",
    "build": "bun build src/server.ts --outdir build --minify",
    "docker:build": "docker build -t metro-now-server ."
  },
  "dependencies": {
    "radash": "^12.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
